{
  "name": "clue",
  "homepage": ".",
  "version": "0.1.0",
  "scripts": {
    "dev": "concurrently \"cd client && PORT=4000 npm run start\" \"cd server && nodemon ./server.js\""
  },
  "devDependencies": {
    "concurrently": "^6.2.0"
  }
}
